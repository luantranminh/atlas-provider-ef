name: CD - Build and Publish NuGet Package

on:
  push:
    branches:
      - master
  pull_request:
  release:
    types:
      - published
  workflow_dispatch:

env:
  # Disable the .NET logo in the console output.
  DOTNET_NOLOGO: true
  # Disable the .NET first time experience to skip caching NuGet packages and speed up the build.
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  # Disable sending .NET CLI telemetry to Microsoft.
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  # Set the build number in MinVer.
  MINVERBUILDMETADATA: build.${{ github.run_number }}

jobs:
  build:
    name: Build on Windows
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.0.0
        with:
          lfs: true
          fetch-depth: 0
      - name: Install .NET Core SDK
        uses: actions/setup-dotnet@v3.2.0
        with:
          dotnet-version: '8.0.x'
      - name: List SDKs
        shell: pwsh
        run: dotnet --list-sdks
      - name: Cache NuGet packages
        uses: actions/cache@v3
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/*.props', '**/*.targets') }}
          restore-keys: |
            ${{ runner.os }}-nuget-
      - name: Cache .NET Tools
        uses: actions/cache@v3
        with:
          path: ./.dotnet/tools
          key: ${{ runner.os }}-dotnet-tools-${{ hashFiles('.config/dotnet-tools.json') }}
          restore-keys: |
            ${{ runner.os }}-dotnet-tools-
      - name: Cache Build Output
        uses: actions/cache@v3
        with:
          path: |
            **/obj
            **/bin
          key: ${{ runner.os }}-build-${{ hashFiles('**/*.csproj', '**/*.props') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-build-${{ hashFiles('**/*.csproj', '**/*.props') }}-
            ${{ runner.os }}-build-
      - name: Dotnet Tool Restore
        shell: pwsh
        run: dotnet tool restore
      - name: Dotnet restore
        shell: pwsh
        run: dotnet restore
      - name: Dotnet Cake CI
        shell: pwsh
        run: dotnet cake --target=CI
      - name: Publish artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: "./artifacts"

  test-tool:
    name: Test tool on ${{ matrix.os }} - ${{ matrix.dialect }}
    needs: build
    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
        dialect: [sqlite]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4.0.0
        with:
          lfs: true
          fetch-depth: 0
      - name: Install .NET Core SDK
        uses: actions/setup-dotnet@v3.2.0
        with:
          dotnet-version: |
            6.0.x
            8.0.x
      - name: Cache NuGet packages
        uses: actions/cache@v3
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-
      - name: Cache .NET Tools
        uses: actions/cache@v3
        with:
          path: ./.dotnet/tools
          key: ${{ runner.os }}-dotnet-tools-${{ hashFiles('.config/dotnet-tools.json') }}
          restore-keys: |
            ${{ runner.os }}-dotnet-tools-
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: "./artifacts"
      - name: Install AtlasEF
        shell: pwsh
        run: dotnet tool install --prerelease --add-source ./artifacts atlas-ef
      - name: Print SQL from Demo project
        shell: pwsh
        working-directory: ./src/Atlas.Provider.Demo
        run: dotnet tool restore && dotnet atlas-ef -- ${{ matrix.dialect }}
      - name: Install Atlas
        uses: ariga/setup-atlas@v0
      - name: Verify Atlas CLI version
        shell: pwsh
        run: atlas version
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Set up Docker
        uses: docker/setup-docker-action@v4
      - name: Cache Docker images
        uses: actions/cache@v3
        with:
          path: /tmp/.docker-cache
          key: ${{ runner.os }}-docker-images-${{ matrix.dialect }}-${{ hashFiles('src/Atlas.Provider.Demo/atlas.hcl') }}
          restore-keys: |
            ${{ runner.os }}-docker-images-${{ matrix.dialect }}-
            ${{ runner.os }}-docker-images-
      - name: Load cached Docker images
        if: matrix.dialect != 'sqlite'
        run: |
          if [ -f /tmp/.docker-cache/${{ matrix.dialect }}.tar ]; then
            echo "Loading cached Docker image for ${{ matrix.dialect }}"
            docker load -i /tmp/.docker-cache/${{ matrix.dialect }}.tar
          fi
      - name: Pre-pull database Docker image
        if: matrix.dialect != 'sqlite'
        run: |
          case "${{ matrix.dialect }}" in
            mysql)
              if ! docker image inspect mysql:8 >/dev/null 2>&1; then
                docker pull mysql:8
                mkdir -p /tmp/.docker-cache
                docker save mysql:8 -o /tmp/.docker-cache/${{ matrix.dialect }}.tar
              fi
              ;;
            postgres)
              if ! docker image inspect postgres:15 >/dev/null 2>&1; then
                docker pull postgres:15
                mkdir -p /tmp/.docker-cache
                docker save postgres:15 -o /tmp/.docker-cache/${{ matrix.dialect }}.tar
              fi
              ;;
            sqlserver)
              if ! docker image inspect mcr.microsoft.com/mssql/server:2022-latest >/dev/null 2>&1; then
                docker pull mcr.microsoft.com/mssql/server:2022-latest
                mkdir -p /tmp/.docker-cache
                docker save mcr.microsoft.com/mssql/server:2022-latest -o /tmp/.docker-cache/${{ matrix.dialect }}.tar
              fi
              ;;
          esac
      - name: Check migrations for ${{ matrix.dialect }}
        working-directory: ./src/Atlas.Provider.Demo
        run: |
          atlas migrate diff --env ef --var dialect=${{ matrix.dialect }}
        env:
          ATLAS_TOKEN: ${{ secrets.ATLAS_TOKEN }}
      - name: Verify migrations generated
        shell: bash
        run: |
          if git status --porcelain | grep -vq 'dotnet-tools.json' ; then
            echo "you need to run 'atlas migrate diff --env ef' and commit the changes"
            git --no-pager diff
            exit 1
          fi
