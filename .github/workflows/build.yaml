name: CD - Build and Publish NuGet Package

on:
  push:
    branches:
      - master
  pull_request:
  release:
    types:
      - published
  workflow_dispatch:

env:
  # Disable the .NET logo in the console output.
  DOTNET_NOLOGO: true
  # Disable the .NET first time experience to skip caching NuGet packages and speed up the build.
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  # Disable sending .NET CLI telemetry to Microsoft.
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  # Set the build number in MinVer.
  MINVERBUILDMETADATA: build.${{ github.run_number }}

jobs:
  build:
    name: Build on Windows
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.0.0
        with:
          lfs: true
          fetch-depth: 0
      - name: Install .NET Core SDK
        uses: actions/setup-dotnet@v3.2.0
        with:
          dotnet-version: '8.0.x'
      - name: List SDKs
        shell: pwsh
        run: dotnet --list-sdks
      - name: Dotnet Tool Restore
        shell: pwsh
        run: dotnet tool restore
      - name: Dotnet restore
        shell: pwsh
        run: dotnet restore
      - name: Dotnet Cake CI
        shell: pwsh
        run: dotnet cake --target=CI
      - name: Publish artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: "./artifacts"

  test-tool:
    name: Test tool on ${{ matrix.os }}
    needs: build
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    env:
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
      DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4.0.0
        with:
          lfs: true
          fetch-depth: 0
      - name: Install .NET Core SDK
        uses: actions/setup-dotnet@v3.2.0
        with:
          dotnet-version: |
            6.0.x
            8.0.x
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: "./artifacts"
      - name: Pack and install AtlasEF tool locally
        shell: bash
        working-directory: ./src/Atlas.Provider.Demo
        run: |
          echo "Packing and installing atlas-ef tool..."
          
          # Uninstall any existing global installation
          dotnet tool uninstall -g atlas-ef || echo "No existing global installation found"
          
          # Restore dependencies first
          dotnet restore ../../src/Atlas.Provider.Loader/Atlas.Provider.Loader.csproj
          
          # Pack the tool project
          dotnet pack ../../src/Atlas.Provider.Loader/Atlas.Provider.Loader.csproj \
            --configuration Release \
            --output ./nupkg \
            --verbosity normal
          
          echo "Packed files:"
          ls -la ./nupkg/
          
          # Install the tool from local package source
          dotnet tool install --add-source ./nupkg -g atlas-ef
          
          # Verify installation
          echo "Testing installed tool..."
          dotnet atlas-ef --help || echo "Help command completed with code $?"
      - name: Compare generated SQL with current_sqlserver.sql
        shell: bash
        working-directory: ./src/Atlas.Provider.Demo
        run: |
          echo "Running atlas-ef command using globally installed tool..."
          echo "Current directory: $(pwd)"
          echo "Checking dotnet version:"
          dotnet --version
          
          echo "Verifying tool installation:"
          dotnet tool list -g | grep atlas-ef || echo "Tool not found in global list"
          
          echo "Testing basic atlas-ef command first..."
          dotnet atlas-ef --help > help_output.txt 2>&1 || echo "Help command failed with code $?"
          echo "Help command output:"
          cat help_output.txt
          
          echo "Now trying the actual command..."
          timeout 60 dotnet atlas-ef -- sqlserver > generated_sqlserver.sql 2>&1
          exit_code=$?
          
          echo "Command exit code: $exit_code"
          
          if [ ! -f "generated_sqlserver.sql" ]; then
            echo "Error: generated_sqlserver.sql was not created"
            exit 1
          fi
          
          echo "File exists. Size: $(wc -c < generated_sqlserver.sql) bytes"
          echo "Generated SQL content:"
          cat generated_sqlserver.sql
          
          # Only fail if the exit code was non-zero AND the file is empty
          if [ $exit_code -ne 0 ] && [ ! -s "generated_sqlserver.sql" ]; then
            echo "Command failed with exit code $exit_code and produced no output"
            exit 1
          fi
          
          if [ ! -f "current_sqlserver.sql" ]; then
            echo "Error: current_sqlserver.sql not found"
            exit 1
          fi
          
          if ! diff -q current_sqlserver.sql generated_sqlserver.sql > /dev/null; then
            echo "Differences found between current_sqlserver.sql and generated SQL:"
            diff current_sqlserver.sql generated_sqlserver.sql
            exit 1
          else
            echo "Generated SQL matches current_sqlserver.sql"
          fi
      - name: Install Atlas CLI
        uses: ariga/setup-atlas@v0
      - name: Verify Atlas CLI version
        shell: pwsh
        run: atlas version
      - name: Check migrations for sqlite
        working-directory: ./src/Atlas.Provider.Demo
        run: |
          atlas migrate diff --env ef --var dialect=sqlite
        env:
          ATLAS_TOKEN: ${{ secrets.ATLAS_TOKEN }}
      - name: Verify migrations generated
        shell: bash
        run: |
          if git status --porcelain | grep -vq 'dotnet-tools.json' ; then
            echo "you need to run 'atlas migrate diff --env ef' and commit the changes"
            git --no-pager diff
            exit 1
          fi
